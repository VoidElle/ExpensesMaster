name: expenses_master
description: A manager for your expenses
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5

  # Persistence
  shared_preferences: ^2.0.18
  sqflite: ^2.2.5

  # Design
  google_fonts: ^4.0.3
  material_design_icons_flutter: ^6.0.7096

  # Utils
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/img/